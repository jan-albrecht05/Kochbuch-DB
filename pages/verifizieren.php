<?php
    //checks the random hash from the verification link with the username in database
    // if verified --> set the user- status as 2 (verified), success message
    // redirect to login page after 5 seconds
    // if not verified --> error message, redirect to login page after 5 seconds
?>